# ===========================================================================
# Copy to .env and fill in. Never commit .env — it is gitignored.
#
# VITE_* variables are compiled into the browser bundle and are PUBLIC.
# Everything else is server-only and must never get a VITE_ prefix.
# ===========================================================================

# --- Supabase (client) ------------------------------------------------------
# Settings -> API in the Supabase dashboard.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# --- Supabase (server) ------------------------------------------------------
# Used by scripts/seed-question-bank.mjs, which writes the question bank.
# The service-role key bypasses row-level security — keep it out of the
# browser and out of version control.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- App --------------------------------------------------------------------
VITE_SUPPORT_EMAIL=support@yourdomain.com
